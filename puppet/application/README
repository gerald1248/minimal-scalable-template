To run rspec puppet tests (plus puppet-lint etc.), enter:

bundle exec rake test

You may need to install the prerequisites as follows:

* rvm install ruby-1.8.7-p374
* gem install bundler
* bundle install --path vendor/gems

The most suitable versions may vary depending on your OS and system configuration.
